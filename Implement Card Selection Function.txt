#Implement Card Selection Function
import tkinter as tk

class Card:
    def __init__(self, name, card_type, points=0):
        self.name = name
        self.card_type = card_type
        self.points = points

class App:
    def __init__(self, master):
        self.master = master
        master.title("Transformers TCG Deck Builder")
        
        # Card list section
        self.card_list_frame = tk.Frame(master)
        self.card_list_frame.pack(side=tk.LEFT, padx=10, pady=10)
        
        self.card_list_label = tk.Label(self.card_list_frame, text="Card List")
        self.card_list_label.pack()
        
        self.card_list = [
            Card("Bumblebee", "Bot", 7),
            Card("Optimus Prime", "Bot", 13),
            Card("Megatron", "Bot", 12),
            Card("Blaster", "Bot", 8),
            Card("Energon Axe", "Upgrade", 3),
            Card("Flamethrower", "Upgrade", 5),
            Card("Hunker Down", "Action", 2),
            Card("Leaping Ahead", "Action", 3),
            Card("Hidden Fortification", "Secret Action", 1),
            Card("New Orders", "Secret Action", 2),
            Card("Ramming Speed", "Battle", 1),
            Card("Brutal Assault", "Battle", 3),
            Card("Disarm", "Battle", 2),
            Card("Bashing Shield", "Battle", 1)
        ]
        
        self.card_list_buttons = []
        for card in self.card_list:
            button = tk.Button(self.card_list_frame, text=f"{card.name} ({card.points})", width=20, 
                               command=lambda card=card: self.add_card_to_selection(card))
            button.pack()
            self.card_list_buttons.append(button)
        
        # Card selection section
        self.card_selection_frame = tk.Frame(master)
        self.card_selection_frame.pack(side=tk.LEFT, padx=10, pady=10)
        
        self.bot_selection_frame = tk.Frame(self.card_selection_frame)
        self.bot_selection_frame.pack(side=tk.TOP)
        
        self.bot_selection_label = tk.Label(self.bot_selection_frame, text="Bot Cards")
        self.bot_selection_label.pack()
        
        self.bot_selection = []
        self.bot_selection_buttons = []
        
        self.battle_selection_frame = tk.Frame(self.card_selection_frame)
        self.battle_selection_frame.pack(side=tk.TOP)
        
        self.battle_selection_label = tk.Label(self.battle_selection_frame, text="Battle Cards")
        self.battle_selection_label.pack()
        
        self.battle_selection = []
        self.battle_selection_buttons = []
        self.battle_selection_quantities = {}
        
        self.stratagem_selection_frame = tk.Frame(self.card_selection_frame)
        self.stratagem_selection_frame.pack(side=tk.TOP)
        
        self.stratagem_selection_label = tk.Label(self.stratagem_selection_frame, text="Stratagem Cards")
        self.stratagem_selection_label.pack()
        
        self.stratagem_selection = []
        self.stratagem_selection_buttons = []
        
        # Card preview section
        self.card_preview_frame = tk.Frame(master, width=400, height=400, bd=1, relief=tk.SOLID)
        self.card_preview_frame.pack(side=tk.LEFT, padx=10, pady=10)
        
        self.card_preview_label = tk.Label(self.card_preview_frame, text="Card Preview")
        self.card_preview_label.pack()
        
        # Totals section
        self.t
